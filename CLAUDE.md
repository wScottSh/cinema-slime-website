## Agent skills

### Issue tracker

GitHub Issues (this repo). See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical mattpocock/skills vocabulary (`needs-triage`, `ready-for-agent`, etc.). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout. Read `CONTEXT.md` at the repo root for the project's ubiquitous language. See `docs/agents/domain.md`.
