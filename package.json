{
  "name": "cinema-slime-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/*.test.js",
    "verify:curation": "node scripts/verify-curation.mjs",
    "publish:curation": "node scripts/publish-curation.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "nostr-tools": "^2.23.5"
  }
}
